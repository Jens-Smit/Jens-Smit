{% extends 'base.html.twig' %}

{% block title %}Arbeitsbereiche index{% endblock %}

{% block body %}
    <h1>Arbeitsbereiche index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bezeichnung</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for arbeitsbereiche in arbeitsbereiches %}
            <tr>
                <td>{{ arbeitsbereiche.id }}</td>
                <td>{{ arbeitsbereiche.Bezeichnung }}</td>
                <td>
                    <a href="{{ path('app_arbeitsbereiche_show', {'id': arbeitsbereiche.id}) }}">show</a>
                    <a href="{{ path('app_arbeitsbereiche_edit', {'id': arbeitsbereiche.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_arbeitsbereiche_new') }}">Create new</a>
{% endblock %}
