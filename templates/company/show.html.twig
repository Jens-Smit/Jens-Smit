{% extends 'base.html.twig' %}

{% block title %}Company{% endblock %}

{% block body %}
    <h1>Company</h1>

    <table class="table" style="color: var(--text-color);">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company.name }}</td>
            </tr>
        </tbody>
    </table>

   

    <a href="{{ path('app_company_edit', {'id': company.id}) }} "class='btn btn-info w-100'>edit</a>

    
{% endblock %}
