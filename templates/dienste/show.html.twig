{% extends 'base.html.twig' %}

{% block title %}Dienste{% endblock %}

{% block body %}
    <h1>Dienste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dienste.id }}</td>
            </tr>
            <tr>
                <th>Kommen</th>
                <td>{{ dienste.kommen ? dienste.kommen|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Gehen</th>
                <td>{{ dienste.gehen ? dienste.gehen|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dienste_index') }}">back to list</a>

    <a href="{{ path('app_dienste_edit', {'id': dienste.id}) }}">edit</a>

    {{ include('dienste/_delete_form.html.twig') }}
{% endblock %}
