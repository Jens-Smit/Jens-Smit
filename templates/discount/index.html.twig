{% extends 'base.html.twig' %}

{% block title %}Discount index{% endblock %}

{% block body %}
    <h1>Discount index</h1>
    {% for discount in discounts %}
{{ discount.itemCategoriesPrices.itemCategories }}
 {% endfor %}
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titel</th>
                <th>Value</th>
                <th>Conditions</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for discount in discounts %}
            <tr>
                <td>{{ discount.id }}</td>
                <td>{{ discount.titel }}</td>
                <td>{{ discount.value }}</td>
                <td>{{ discount.conditions }}</td>
                <td>
                    <a href="{{ path('app_discount_show', {'id': discount.id}) }}">show</a>
                    <a href="{{ path('app_discount_edit', {'id': discount.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_discount_new', {'categoryId': categoryId.id}) }}">Create new</a>
{% endblock %}
