<form method="post" action="{{ path('app_fehlzeiten_delete', {'id': fehlzeiten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fehlzeiten.id) }}">
    <button class="btn">Delete</button>
</form>
