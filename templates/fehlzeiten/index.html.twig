{% extends 'base.html.twig' %}

{% block title %}Fehlzeiten index{% endblock %}

{% block body %}
    <h1>Fehlzeiten index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bezeichnung</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fehlzeiten in fehlzeitens %}
            <tr>
                <td>{{ fehlzeiten.id }}</td>
                <td>{{ fehlzeiten.bezeichnung }}</td>
                <td>
                    <a href="{{ path('app_fehlzeiten_show', {'id': fehlzeiten.id}) }}">show</a>
                    <a href="{{ path('app_fehlzeiten_edit', {'id': fehlzeiten.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fehlzeiten_new') }}">Create new</a>
{% endblock %}
