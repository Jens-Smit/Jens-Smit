{% extends 'base.html.twig' %}

{% block title %}ItemCategories{% endblock %}

{% block body %}
    <h1>ItemCategories</h1>

    <table class="table" style="color: var(--text-color);">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_category.name }}</td>
            </tr>
            <tr>
                <th>Booktime</th>
                <td>{{ item_category.booktime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_item_categories_index') }}">back to list</a>

    <a href="{{ path('app_item_categories_edit', {'id': item_category.id}) }}">edit</a>

    {{ include('item_categories/_delete_form.html.twig') }}
{% endblock %}
