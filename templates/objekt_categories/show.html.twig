{% extends 'base.html.twig' %}

{% block title %}ObjektCategories{% endblock %}

{% block body %}
    <h1>ObjektCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objekt_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ objekt_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objekt_categories_index') }}">back to list</a>

    <a href="{{ path('app_objekt_categories_edit', {'id': objekt_category.id}) }}">edit</a>

    {{ include('objekt_categories/_delete_form.html.twig') }}
{% endblock %}
