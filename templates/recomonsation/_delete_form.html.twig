<form method="post" action="{{ path('app_recomonsation_delete', {'id': recomonsation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recomonsation.id) }}">
    <button class="btn w-100 btn-info">Delete</button>
</form>
