{% extends 'base.html.twig' %}

{% block title %}Hello ReservationsController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    
<br>
     <h1>{{ info }}</h1>
     {{ form_start(ReservationForm) }}
   {{ form_widget(ReservationForm) }}
    {{ form_end(ReservationForm) }}
    {% if items is defined  %}
  
  <section class="playlist-section spad">
		<div class="container-fluid">
			<div class="section-title">
				<h2>Menükarte</h2>
			</div>
			<div class="container">
				<ul class="playlist-filter controls">
        {% for Category in Categories %}
        <li class="control" data-filter=".{{Category.id}}">{{Category.name}}</li>
         {% endfor %}
				</ul>
			</div>
			<div class="clearfix"></div>
			<div class="row playlist-area">
				{% for item in items %}
					<div class="mix col-lg-3 col-md-4 col-sm-6 {{item.category_id}}">
						<div class="playlist-item">
							<h5>{{item.objekt_id}}</h5>
							<p>test
								<a class="btn btn-outline-danger btn-sm" href="#" role="button">Bestellen</a>
							</p>
						</div>
					</div>
				{% endfor %}

			</div>
		</div>
	</section>

  
    
   {% endif %}
    
     
</div>
{% endblock %}
